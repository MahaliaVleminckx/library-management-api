@model List<Pri.Ee.Client.Models.Loans.LoanViewModel>

<h2 class="page-title mb-4">Loans</h2>

<div class="table-container">

    <table class="table table-custom">

        <thead>
            <tr>
                <th>Id</th>
                <th>Book</th>
                <th>User</th>
                <th>Loan Date</th>
                <th>Return Date</th>
                <th>Actions</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var loan in Model)
            {
                <tr>
                    <td>@loan.Id</td>

                    <td class="book-title">
                        @(loan.BookTitle ?? "Unknown book")
                    </td>

                    <td>
                        @(loan.UserName ?? loan.UserId)
                    </td>

                    <td>
                        @loan.LoanDate.ToShortDateString()
                    </td>

                    <td>
                        @(loan.ReturnDate.HasValue
                            ? loan.ReturnDate.Value.ToShortDateString()
                            : "Not returned")
                    </td>

                    <td class="actions-cell">

                        <a class="btn btn-sm btn-action btn-info-custom"
                           asp-controller="Loans"
                           asp-action="Details"
                           asp-route-id="@loan.Id">
                            Details
                        </a>

                        <a class="btn btn-sm btn-action btn-edit-custom"
                           asp-controller="Loans"
                           asp-action="Edit"
                           asp-route-id="@loan.Id">
                            Edit
                        </a>

                        <a class="btn btn-sm btn-action btn-delete-custom"
                           asp-controller="Loans"
                           asp-action="Delete"
                           asp-route-id="@loan.Id"
                           onclick="return confirm('Are you sure?')">
                            Delete
                        </a>

                    </td>
                </tr>
            }
        </tbody>

    </table>

</div>

<p>
    <a class="btn btn-create-custom mt-3"
       asp-controller="Loans"
       asp-action="Create">
        + Create New Loan
    </a>
</p>
